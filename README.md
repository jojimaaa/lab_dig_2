# NeuroSync

Entre no diretório do Neurosync Python
```
cd neurosync-python
```

<br/>

Rode os seguintes comandos
```powershell
# Instalar venv
py -m venv .venv

# Ativar venv
./.venv/Scripts/Activate.ps1

# Instalar dependências
pip install -r requirements.txt
```